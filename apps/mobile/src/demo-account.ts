/**
 * The pre-onboarded development account the dev server creates on boot.
 *
 * Kept in step with `DEMO_ACCOUNT` in `server/src/accounts.ts` by hand: the app
 * and the server are separate bundles, and one shared constant is not worth a
 * package between them. If you change it there, change it here.
 */
export const DEMO_EMAIL = "demo@calora.local";
